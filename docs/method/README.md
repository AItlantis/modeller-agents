# Method

The method layer stores task shapes, templates, checklists, and workflows. It does not store executable pipeline knowledge.

Pipeline execution belongs to a registered backend. Pipeline schemas belong to `modeller-pipelines`.

