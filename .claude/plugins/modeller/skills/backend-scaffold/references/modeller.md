# Backend Scaffold Reference Pack

All contract artifacts (minirunner.py, result_builder.py, conformance kit, schemas) are sourced from the resolved modeller-pipelines vendor checkout at the pinned contract ref. Never embed contract content in this skill or its reference pack. Load `reference-packs/modeller-pipelines.toml` for vendor path resolution, contract.lock format, and invariants.
