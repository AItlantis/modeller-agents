# Source Boundary Reference Pack

Accepted ownership baseline:

- Testudo owns product cockpit and product UI.
- `modeller-agents` owns central reusable skills and runtime routing.
- `modeller-memory` owns generated memory tooling.
- `modeller-pipelines` owns pipeline and backend contract schemas.
- `modelling-knowledge` owns durable maps, standards, decisions, and domain notes.
- `aimsun-psp` owns active Aimsun backend implementation.

