# Workflow Reference Pack

The default workflow follows:

```text
context envelope -> repository selection -> skill selection -> knowledge retrieval -> optional memory query -> source-boundary check -> proposal or bounded action -> structured result
```

Sensitive actions require confirmation before execution.

