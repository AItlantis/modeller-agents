# Pipeline Review Reference Pack

Review validates against the modeller-pipelines contract via the resolved vendor checkout schemas — never embedded field enumerations. Backend-specific invariants come from the target backend's reference pack, not from this skill body. Load `reference-packs/modeller-pipelines.toml` for schema paths and invariants.
