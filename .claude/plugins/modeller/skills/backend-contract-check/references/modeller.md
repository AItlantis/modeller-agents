# Backend Contract Reference Pack

`aimsun-psp` is a runtime backend, not a vendored subtree. `modeller-pipelines` owns the generic contract schemas. `modeller-agents` owns the runtime backend registry and validates before invocation.

