# Backend Align Reference Pack

Alignment compares the backend's contract.lock to the vendor/modeller-pipelines contracts/VERSION and composes pipeline-review for the conformance gap. All schema validation references the resolved modeller-pipelines checkout — never schema field enumerations embedded here. Load `reference-packs/modeller-pipelines.toml` for vendor path resolution and invariants.
