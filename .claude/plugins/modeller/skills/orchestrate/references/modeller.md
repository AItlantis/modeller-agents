# Orchestration Reference Pack

Use the source-of-truth split from `modelling-knowledge/registry/repositories.yml`.

Repository-local agents stay in their source repositories. This plugin selects reusable methods and reference packs; it does not replace local authority.

